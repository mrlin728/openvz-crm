{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "What the loading window may do while the servers start.",
	"windows": ["main"],
	"permissions": ["core:default", "core:event:default"]
}
